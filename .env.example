DATABASE_URL=postgresql+psycopg://ledger:ledger@localhost:5432/ledger
SECRET_KEY=change-me
SEED=20260723

# stub = deterministic fabricated AI responses, no network, no key needed.
# live = real Anthropic calls, same schema, falls back to stub on any error.
AI_MODE=stub
ANTHROPIC_API_KEY=
AI_MODEL=claude-sonnet-4-6

# Optional. When set, /welcome links the "Simulated login" footer to the README.
# Vite only — restart `npm run dev` after changing.
# VITE_REPO_URL=https://github.com/VarunnReddyy/ledger
